import 'package:flutter/material.dart';

class TrackerCameraPage extends StatelessWidget {
  const TrackerCameraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
